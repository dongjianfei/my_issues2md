---
allowed-tools: Bash(make build)
---

Run `make build` to compile the project. If the build fails, analyze the error output and identify the root cause.

Steps:
1. Execute `make build`
2. If successful, report which binaries were produced
3. If failed, analyze the compiler errors and suggest fixes
